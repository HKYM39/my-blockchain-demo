syntax = "proto3";
package ncoin.ncoin.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "ncoin/ncoin/v1/params.proto";

option go_package = "N-Coin/x/ncoin/types";

// GenesisState defines the ncoin module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
